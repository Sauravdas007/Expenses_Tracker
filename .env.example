# For local dev (SQLite file)
DATABASE_URL="file:./dev.db"

# For Vercel production, swap to Postgres (Neon/Vercel Postgres) or Turso.
# Example (Postgres):
# DATABASE_URL="postgresql://USER:PASS@HOST:5432/db?sslmode=require"
# And change `provider = "postgresql"` in prisma/schema.prisma

NEXT_PUBLIC_APP_NAME="Paisa — Expense Tracker"
